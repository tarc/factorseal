# Notes for coding agents

- Build, test, and feature flags: `docs/development.md`.
- Windows builds and checks of FactorSeal Desktop, driven from WSL2:
  `scripts/windows-desktop-check/README.md`. Desktop cannot be
  cross-compiled from Linux; build the Windows copy with
  `scripts/windows-desktop-check/build-windows.sh`.
